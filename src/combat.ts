export function createCombatant(kind,x,y,boss=false){const defs={ashling:[24,5,0],glassMite:[18,4,0],sparkWarden:[30,7,5],ashenHound:[28,7,0],veilMoth:[20,5,4],rootBrute:[48,9,0],coilStalker:[34,8,2],cinderWisp:[16,6,5],hollowMarshal:[85,10,0],riftColossus:[180,15,3]},[hp,damage,range]=defs[kind]||defs.ashling;return{id:`${kind}-${x}-${y}`,kind,x,y,hp,maxHp:hp,damage,range,boss,cooldown:0,telegraph:0,dead:false}}
export function dodge(player,now,x=0,y=0){if(player.stamina<15)return{ok:false,reason:'stamina'};let m=Math.hypot(x,y);if(m<.15){const q=player.lastMoveVector||{x:0,y:1};x=Number(q.x)||0;y=Number(q.y)||0;m=Math.hypot(x,y)||1}player.dodgeX=x/m;player.dodgeY=y/m;player.lastMoveVector={x:player.dodgeX,y:player.dodgeY};player.stamina-=15;player.invulnerableUntil=now+420;player.dodgeUntil=now+260;return{ok:true}}
export function playerAttack(player,enemies,now){if(now<(player.attackReadyAt||0))return null;player.attackReadyAt=now+420;let best=null,dist=1.65;for(const e of enemies){const d=Math.hypot(e.x-player.x,e.y-player.y);if(!e.dead&&d<dist){best=e;dist=d}}if(best){best.hp-=7+(player.stats.Might*2)+(player.weaponLevel*3);best.hitFlash=.18;best.recoil=.14;if(best.hp<=0)best.dead=true}return best}
