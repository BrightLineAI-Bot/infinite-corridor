export function normalizeVector(x,y){const m=Math.hypot(x,y);return m>1?{x:x/m,y:y/m}:{x,y}}
export function shapeStick(x,y,dead=.16){const raw=Math.hypot(x,y);if(raw<=dead)return{x:0,y:0};const magnitude=Math.min(1,(raw-dead)/(1-dead))**2,scale=magnitude/raw;return{x:x*scale,y:y*scale}}
export function smoothAxis(current,target,dt,response=18){return current+(target-current)*(1-Math.exp(-response*Math.max(0,dt)))}
export function createInput(root,host=globalThis){const state={x:0,y:0,attack:false,tool:false,spell:false,dodge:false,jump:false,interact:false,potion:false,menu:false,pause:false,map:false,journal:false},target={x:0,y:0},keys=new Set,latch=new Set,stick=root.querySelector('#stick'),nub=stick.querySelector('i');let pointer=null;const actions={j:'attack',q:'tool',r:'tool',f:'spell',' ':'dodge',k:'jump',e:'interact',h:'potion',p:'pause',escape:'pause',m:'map',i:'menu',l:'journal'};function reset(){keys.clear();latch.clear();pointer=null;target.x=target.y=state.x=state.y=0;for(const k in state)if(k!=='x'&&k!=='y')state[k]=false;nub.style.transform='translate(0px,0px)'}function keydown(e){const k=e.key.toLowerCase();keys.add(k);if(actions[k]&&!e.repeat)latch.add(actions[k]);if([' ','arrowup','arrowdown','arrowleft','arrowright'].includes(k))e.preventDefault()}function move(e){if(pointer!==e.pointerId)return;const b=stick.getBoundingClientRect(),r=b.width*.38,v=shapeStick((e.clientX-b.left-b.width/2)/r,(e.clientY-b.top-b.height/2)/r);target.x=v.x;target.y=v.y;nub.style.transform=`translate(${v.x*r*.58}px,${v.y*r*.58}px)`}stick.onpointerdown=e=>{pointer=e.pointerId;stick.setPointerCapture?.(pointer);move(e)};stick.onpointermove=move;const release=e=>{if(pointer===null||!e||e.pointerId===pointer)reset()};stick.onpointerup=release;stick.onpointercancel=release;stick.onlostpointercapture=release;host.addEventListener?.('keydown',keydown);host.addEventListener?.('keyup',e=>keys.delete(e.key.toLowerCase()));host.addEventListener?.('blur',reset);root.addEventListener?.('visibilitychange',()=>{if(root.hidden)reset()});root.querySelectorAll('[data-action]').forEach(b=>b.onpointerdown=e=>{e.preventDefault();latch.add(b.dataset.action)});return{state,reset,update(dt=1/60){if(pointer===null){const v=normalizeVector((keys.has('d')||keys.has('arrowright')?1:0)-(keys.has('a')||keys.has('arrowleft')?1:0),(keys.has('s')||keys.has('arrowdown')?1:0)-(keys.has('w')||keys.has('arrowup')?1:0));target.x=v.x;target.y=v.y}state.x=smoothAxis(state.x,target.x,dt);state.y=smoothAxis(state.y,target.y,dt);for(const a of latch)state[a]=true;latch.clear()},consume(k){const v=state[k];state[k]=false;return v}}}
