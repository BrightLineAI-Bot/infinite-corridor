Project-owned portable contracts. External coordinator owns effect history.
Do not regenerate projectId or clear state to bypass a conflict. Component commands are unconfigured until the project implements and tests them.
