import http from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,join,resolve}from'node:path';const root=process.cwd(),types={'.html':'text/html','.css':'text/css','.js':'text/javascript','.ts':'text/javascript','.svg':'image/svg+xml','.webmanifest':'application/manifest+json'};http.createServer(async(req,res)=>{try{let p=req.url==='/'?'index.html':decodeURIComponent(req.url.slice(1));p=p.replace(/^src\/(.+)\.js$/,'src/$1.ts');const full=resolve(root,p);if(!full.startsWith(root))throw 0;let data=await readFile(full);if(extname(full)==='.ts')data=Buffer.from(data.toString().replaceAll(".ts'",".js'"));res.setHeader('content-type',types[extname(full)]||'application/octet-stream');res.end(data)}catch{res.statusCode=404;res.end('Not found')}}).listen(5173,'0.0.0.0',()=>console.log('Infinite Corridor at http://localhost:5173'));
